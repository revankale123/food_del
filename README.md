🍕 Food Delivery System

A simple full-stack food ordering web application built using the MERN stack.
Users can browse food items, add them to the cart, place orders, and track order status.
Admins can manage menu items and customer orders.

✨ Features
👤 User

Register and Login

Secure JWT authentication

View food menu

Add items to cart

Increase or decrease quantity

Place orders

View and track order status


Run Backend
cd backend
npm install
npm start


Set up .env file:

MONGO_URI=your_mongo_connection
JWT_SECRET=your_secret_key
PORT=5000

Run Frontend
cd frontend
npm install
npm start
